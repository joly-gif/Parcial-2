# Pega a base e vira decimal
b = float(input("Base: "))
# Pega a altura e vira decimal
h = float(input("Altura: "))
# Faz a conta da área do triângulo e já imprime direto
print("A área é:", (b * h) / 2)